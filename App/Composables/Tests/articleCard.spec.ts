import { expect, test, describe } from 'vitest'



describe('Testing articleCard', () => {

    test('Can construct article with custome md text', () => {

    });

    test('articleCard is small when unselected', () => {

    });

    test('articleCard is large when selected', () => {

    });

    test('articleCard can dynamicly change size, when selected, then unselected', () => {

    });
});